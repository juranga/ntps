import queue
from queue import * 

class InterceptQueue(Queue):

    self.queue = Queue(100)

    def init_intercept_queue(size=100):
        intercept_queue = Queue(size)

    def change_queue_size():
        #TODO: Code to populate new queue with old queue, but larger size.

